# Green stack upstream
upstream backend_upstream {
    server dzcash-green_backend_1:4000 max_fails=3 fail_timeout=30s;
}

upstream frontend_upstream {
    server dzcash-green_frontend_1:3000 max_fails=3 fail_timeout=30s;
}
