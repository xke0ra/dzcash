# Blue stack upstream
upstream backend_upstream {
    server dzcash-blue_backend_1:4000 max_fails=3 fail_timeout=30s;
}

upstream frontend_upstream {
    server dzcash-blue_frontend_1:3000 max_fails=3 fail_timeout=30s;
}
